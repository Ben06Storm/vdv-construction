export type ContactFormData = {
  name: string;
  phone: string;
  email: string;
  project: string;
  message: string;
};

export type ServiceRequestFormData = {
  name: string;
  phone: string;
  email: string;
  message: string;
};

export type ReviewFormData = {
  name: string;
  city: string;
  rating: number;
  review: string;
};